fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic-settings==2.5.0
email-validator==2.2.0
python-multipart==0.0.12
python-dotenv==1.0.1
slowapi==0.1.9
lightgbm==4.5.0
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3
# Escribe el .xlsx del reporte de indicadores. Estaba disponible en local
# como extra de pandas, pero sin declararlo aquí Render no lo instala y la
# descarga del reporte responde un 500.
openpyxl==3.1.5
mercadopago==3.5.0
# Las llamadas a Niubiz, que no tiene SDK de Python. Asíncronas, porque el
# servidor corre con un solo proceso y una espera bloqueante contra la
# pasarela dejaría clavada la tienda entera mientras tanto.
httpx==0.28.1
# Guarda las imagenes del panel de administracion fuera del servidor: en el
# plan gratuito de Render el disco es efimero y en la base de datos ocuparian
# el poco espacio del plan gratuito de Neon.
cloudinary==1.41.0
