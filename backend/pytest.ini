[pytest]
testpaths = tests
pythonpath = .
# Las pruebas de la API son corrutinas; con este modo no hace falta marcar
# cada una con @pytest.mark.asyncio.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
